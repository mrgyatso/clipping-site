# AGENTS.md

The rules for this repo live in **[CLAUDE.md](./CLAUDE.md)** — one file so the
two of us cannot drift apart on them.

**Read `CLAUDE.md` now, before making any edit.** It is short, and it covers the
git workflow, which files cause phantom merge conflicts, who owns which parts of
the codebase, and the design-token rules.
